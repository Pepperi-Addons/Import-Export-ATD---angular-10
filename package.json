{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ./client-side && npm run build && cd .. && cd ./server-side && npm run build && cd ..",
    "init": "npm install && cd ./server-side && npm install && cd .. && cd ./client-side && npm i --force && cd ..",
    "publish-addon": "npm run build && publish-addon",
    "pub-addon": "publish-addon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pepperi-addons/papi-sdk": "^1.47.0",
    "@pepperi-addons/scripts": "^2.0.4",
    "@types/node-fetch": "^2.5.7",
    "again": "0.0.1"
  }
}
